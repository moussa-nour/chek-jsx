import react from 'react';

const Image = ({ src, alt, caption }) => {
    return (
      <div>
        <img src ={'./montre2.jpg'} alt={alt} />
       
      </div>
    );
  };
  
  export default Image;